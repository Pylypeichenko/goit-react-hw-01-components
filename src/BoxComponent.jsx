import styled from 'styled-components';
import { border, color, flexbox, layout, shadow, space } from 'styled-system';

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
`;

export default Box;
